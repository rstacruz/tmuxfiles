#!/usr/bin/env bash
# ping=$(ping 8.8.8.8 -c 1 | head -n 2 | tail -n 1 | sed 's/^.*time=//g' | sed 's/\.[0-9]\+ //')
# status=$(~/.asdf/shims/timesense --status)
# date=$(date +"%I:%M:%S %p" | tr /A-Z/ /a-z/)
# echo -e "#[fg=#aaaaaa]${status} #[fg=#333333]· ${date} · ${ping}"
